# Railway: https://docs.railway.com/guides/deploy
# Start command берётся из package.json в корне проекта → npm start

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
